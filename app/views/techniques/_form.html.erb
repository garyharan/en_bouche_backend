<%= form_with(model: technique, html: { class: "bg-gray-100 p-4 rounded-lg" }) do |form| %>
  <% if technique.errors.any? %>
    <div style="color: red" class="mb-4">
      <h2 class="text-red-500"><%= pluralize(technique.errors.count, "error") %> prohibited this technique from being saved:</h2>

      <ul>
        <% technique.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="mb-4">
    <%= form.label :name, style: "display: block", class: "text-gray-700 font-bold" %>
    <%= form.text_field :name, class: "border border-gray-300 rounded-lg p-2 w-full" %>
  </div>

  <div class="mb-4">
    <%= form.label :description, style: "display: block", class: "text-gray-700 font-bold" %>
    <%= form.text_area :description, class: "border border-gray-300 rounded-lg p-2 w-full" %>
  </div>

  <div>
    <%= form.submit "Save", class: "bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded" %>
  </div>
<% end %>
